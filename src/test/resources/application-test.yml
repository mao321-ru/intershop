spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/intershopdb_test

  # Run SQL script to initialize database schema and add test data
  sql.init.data-locations: classpath:test-data.sql
